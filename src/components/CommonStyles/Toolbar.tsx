import React from 'react';
import ToolbarMui, { ToolbarProps } from '@mui/material/Toolbar';

const Toolbar = (props: ToolbarProps) => {
  //! State

  //! Function

  //! Render

  return <ToolbarMui {...props} />;
};

export default React.memo(Toolbar);
